# Your Keras model definition and training code here
# Once your model is trained, save it using the save_model function

from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Dense, Flatten
from save import save_model  # Import the save_model function

# Example model architecture (replace with your actual model)
model = Sequential([
    Flatten(input_shape=(256, 256, 3)),
    Dense(128, activation='relu'),
    Dense(1, activation='sigmoid')
])

# Compile the model (replace with your compile settings)
model.compile(optimizer='adam', loss='binary_crossentropy', metrics=['accuracy'])

# Train your model (replace with your training code)
# model.fit(X_train, y_train, epochs=10, validation_data=(X_val, y_val))

# Once your model is trained, save it using the save_model function

# Define the path where you want to save the model
model_path = "final1.h5"

# Save the model
save_model(model, model_path)
